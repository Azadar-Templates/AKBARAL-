{
  "$comment": [
    "Synthetic credentials that legitimately appear in fixtures, unit tests and documentation.",
    "Two entry kinds:",
    "  { \"value\": \"<literal>\", \"reason\": \"...\" }  — exact literal substring, allowed anywhere.",
    "  { \"path\": \"<relative path>\", \"reason\": \"...\" } — scoped to one file, and ONLY for the",
    "      placeholder-prone 'connection-string-password' pattern. Key-shaped markers",
    "      (sk-*, AIza*, gh*_*, xox*, AKIA*, tvly-*, PEM private-key blocks, bearer tokens)",
    "      can never be exempted by path, so a real key pasted into a test file still fails.",
    "The scanner still FAILS on every marker that is not covered here.",
    "Run `npm run scan:secrets -- --strict` to ignore this file entirely (audit mode).",
    "Never add a value or path that could hide a real credential."
  ],
  "allow": [
    {
      "path": "src/db/pg-connection.test.ts",
      "reason": "DSN-normalization unit test: every connection string in this file is a synthetic placeholder (u:p@ hosts exercising sslmode rewrite logic)."
    },
    {
      "path": "src/db/postgres.integration.test.ts",
      "reason": "Engine-detection fixture: single-character user/password used only to assert postgres:// is recognized."
    },
    {
      "path": "scripts/pg-test-server.mjs",
      "reason": "Local pglite wire-protocol test server DSN (postgres:postgres@127.0.0.1) started on loopback for the integration test."
    },
    {
      "path": ".env.example",
      "reason": "Template documentation: the literal words 'user' and 'password' in the commented production example."
    },
    {
      "path": "deploy/modal/README.md",
      "reason": "Runbook documentation: USER/PASSWORD/HOST are the shell-style tokens the operator replaces."
    },
    {
      "value": "AIzaAbCdEfGh123456789012345678",
      "reason": "Redaction fixture: a sequential fake Google key used to assert masking never leaks it."
    },
    {
      "value": "Bearer test-fixture-key",
      "reason": "Redaction fixture asserting the safe logger masks bearer material."
    }
  ]
}
