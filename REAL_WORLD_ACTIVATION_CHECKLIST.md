# REAL-WORLD ACTIVATION CHECKLIST — 2026-09-20 (Asia/Karachi)
> Branch `arena/01a0bdde-akbaral` HEAD `a924e4a` | Generated 2026-09-20T15:16:57.509037 | `ZA141251SA_DATABASE_URL=mission.db` (empty provider_readiness — seeded logically)

**Constraint**: Do NOT modify architecture/engine code. This checklist is read-only inspection of `src/mission/earning/platform-connectors.ts`, `opportunity-registry.ts`, `provider-capability-registry.ts`, `db/migrations-mission/0031_provider_readiness.sql`, and `mission.db` state. No secrets requested, no fake revenue/accounts.

## 0) Exact current counts (DB truth)

- `PLATFORM_CONNECTORS` total: **57**
  - `EARNING_SOURCE`: **36**
  - `RESTRICTED_HUMAN_ONLY`: **3**
  - `INFRASTRUCTURE`: **10**
  - `PAYMENT_RAIL`: **4**
  - `TOOL`: **3**
  - `NOT_AN_EARNING_SOURCE`: **1**
- **Permitted EARNING_SOURCE**: **36** (not 1000+; honest count from `platform-connectors.ts` `kind==='EARNING_SOURCE'`)
- `OPPORTUNITY_REGISTRY` classes: **24** (24 scored classes beyond 94 integrations, see `opportunity-registry.ts:701`)
- `mission_provider_readiness` rows in `mission.db`: **0** (not yet seeded — status derived honestly via `connectorToReadiness()` with no vault credentials, no `process.env` keys)
- `mission_payout_slots` status: `unconfigured` (4 slots) — verified via `mission.db` (no `active` slot)
- `mission_credentials` rows: **0** — no PAT/API key stored

> **All 36 EARNING_SOURCE are NOT ACTIVATED** — no external provider evidence exists. USD verified earnings = `$0` (see `owner-command-center.ts:verifiedPayments`).

## 1) Category separation (honest derived status)

| Category | Count | IDs | Meaning |
|---|---|---|---|
| **READY** (automation permitted, no account required) | 5 | direct_client_research, direct_ai_implementation, direct_automation, direct_consulting, seo_direct | `requiresOwnerAccount=false` + `apiPermitted=true` + `status=ACTIVE` — lawful-purpose verified work only; still blocked on payout-slot verification |
| **OWNER ACTION REQUIRED** (`not_configured`) | 29 | upwork, fiverr, freelancer, toptal, contra, peopleperhour, guru, hackerone, bugcrowd, yeswehack, intigriti, kaggle, topcoder, devpost, rapidapi, aws_data_exchange, gumroad, lemon_squeezy, etsy_digital, shopify_app_store, atlassian_marketplace, wordpress_plugin, github_sponsors, open_collective, amazon_associates, awin, shareasale, testio, gengo | `requiresOwnerAccount=true`, no vault credential, `ACTIVE`/`PERMITTED` — human must create account + KYC + payout + store credential |
| **HUMAN ONLY** (`RESTRICTED_HUMAN_ONLY` kind) | 3 | creator_youtube, shopify_store, mturk | `kind=RESTRICTED_HUMAN_ONLY` — platform ToS prohibits automation, human must complete task |
| **COUNTRY/KYC RESTRICTED** (`status=RESTRICTED` + `EARNING_SOURCE`) | 2 | udemy, preply | `status=RESTRICTED` — additional owner setup / restricted per `platform-connectors.ts` (e.g. live tutoring, instructor publishing) |
| **BLOCKED** (`status=BLOCKED`) | 2 | mturk, example_not_earning | `status=BLOCKED` — explicitly blocked, cannot be pursued |
| **NOT AN EARNING SOURCE** | 1 | example_not_earning | `kind=NOT_AN_EARNING_SOURCE` — infra/payment/tool/demo, never counted as earnings |
| **INFRASTRUCTURE** (reference) | 10 | github, gitlab, docker, aws, cloudflare, vercel, neon, supabase, openai, anthropic | Tooling only (GitHub, Docker, AWS, Cloudflare, Vercel, Neon, Supabase, OpenAI, Anthropic, GitLab) — not earnings |
| **PAYMENT_RAIL** (reference) | 4 | stripe, paypal, payoneer, wise | Payout rails only (Stripe, PayPal, Payoneer, Wise) |
| **TOOL** | 3 | sentry, datadog, tavily | Observability/search only (Sentry, Datadog, Tavily) |

## 2) Per-source 14-point checklist — all 36 permitted EARNING_SOURCE

> For Pakistan availability: **PayPal is NOT officially available in Pakistan** — Pakistani residents cannot open local PayPal account / receive PayPal balance [survivezeal.com](https://survivezeal.com/paypal-pakistan/)[nsave.com](https://www.nsave.com/pakistan/paypal-alternative). **Fiverr in Pakistan IS available** via Payoneer / bank transfer (PayPal unavailable) [nsave.com](https://www.nsave.com/pakistan/fiverr). **Upwork IS available in Pakistan** — Pakistan listed as supported freelancer country with active profiles [upwork.com](https://www.upwork.com/hire/front-end-developers/pk/). All other platforms: verify on `officialUrl` below; no payout via PayPal where listed as unavailable.

### 1. Upwork (`upwork`)

- **1. Platform**: Upwork — `https://support.upwork.com/hc/en-us/articles/10408677826963` (Official: catalog/escrow 10-15% fee, payout via PayPal/Payoneer/bank, ToS prohibits automated bidding but permits AI-assisted work after assignment)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: seller account creation, KYC/tax, listing publish (manual), bidding/proposal send, contract acceptance/negotiation, payout withdrawal — class `freelance_client_work`
- **3. Pakistan availability**: Yes — Pakistan is supported seller country; payout via Payoneer / Wise / bank works in PK (PayPal route unavailable). See Fiverr PK via Payoneer [nsave.com](https://www.nsave.com/pakistan/fiverr) and Upwork PK profiles [upwork.com](https://www.upwork.com/hire/front-end-developers/pk/).
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; platform may request bank/card statement, skills verification (Toptal screening)
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E; platform issues 1099-K only for US persons; PK freelancer self-declares via NTN
- **7. Payout method**: Client escrow → platform fee → seller payout | Platform payout to linked PayPal/Payoneer/Wise / bank; settlement verified via payout-verification adapter + receiving rail proof before entering mission cash; platform fee 5-20% documented. (evidence: Official: catalog/escrow 10-15% fee, payout via PayPal/Payoneer/bank, ToS prohibits automated bidding but permits AI-assisted work after assignment) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://support.upwork.com/hc/en-us/articles/10408677826963`
- **9. Automation allowed**: NO — Agents may NOT auto-bid or auto-create listings; platform ToS requires human seller identity and owner-approved engagement. Agents may autonomously PERFORM scoped work after owner assigns one agent to one opportunity. (apiPermitted=True, status=ACTIVE, class=freelance_client_work)
- **10. Human-only actions**: seller account creation, KYC/tax, listing publish (manual), bidding/proposal send, contract acceptance/negotiation, payout withdrawal | Class humanControlled: `Owner creates/owns seller account (real identity, KYC, tax), publishes listing (manual), approves scope/price, accepts c`
- **11. Credentials/config needed**: `UPWORK_PAT` in `process.env` OR vault `mission_credentials` provider=`upwork`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `UPWORK_PAT` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=ACTIVE, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates Upwork seller account at upwork.com with real identity (CNIC/passport), completes KYC, links Payoneer (not PayPal) for payout, publishes profile (manual). 2) In Mission vault: `POST /api/credentials` store `UPWORK_PAT` (GraphQL PAT) — do NOT paste in chat. 3) In Mission dashboard: verify payout slot (1-4) via `configurePayoutSlot` + `confirmPayoutVerification` (checks + attestation). 4) No bidding automation — human approves every proposal send.

### 2. Fiverr (`fiverr`)

- **1. Platform**: Fiverr — `https://help.fiverr.com/hc/en-us/articles/360020164118` (Official: gig fees 20%, PayPal/Payoneer payouts; ToS requires human seller identity)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: account/KYC, gig publish, order acceptance, payout — class `freelance_client_work`
- **3. Pakistan availability**: Yes — Pakistan is supported seller country; payout via Payoneer / Wise / bank works in PK (PayPal route unavailable). See Fiverr PK via Payoneer [nsave.com](https://www.nsave.com/pakistan/fiverr) and Upwork PK profiles [upwork.com](https://www.upwork.com/hire/front-end-developers/pk/).
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; platform may request bank/card statement, skills verification (Toptal screening)
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E; platform issues 1099-K only for US persons; PK freelancer self-declares via NTN
- **7. Payout method**: Gig order → escrow → payout | Platform payout to linked PayPal/Payoneer/Wise / bank; settlement verified via payout-verification adapter + receiving rail proof before entering mission cash; platform fee 5-20% documented. (evidence: Official: gig fees 20%, PayPal/Payoneer payouts; ToS requires human seller identity) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://help.fiverr.com/hc/en-us/articles/360020164118`
- **9. Automation allowed**: NO — Agents may NOT auto-bid or auto-create listings; platform ToS requires human seller identity and owner-approved engagement. Agents may autonomously PERFORM scoped work after owner assigns one agent to one opportunity. (apiPermitted=True, status=ACTIVE, class=freelance_client_work)
- **10. Human-only actions**: account/KYC, gig publish, order acceptance, payout | Class humanControlled: `Owner creates/owns seller account (real identity, KYC, tax), publishes listing (manual), approves scope/price, accepts c`
- **11. Credentials/config needed**: `FIVERR_API_KEY` in `process.env` OR vault `mission_credentials` provider=`fiverr`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `FIVERR_API_KEY` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=ACTIVE, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates Fiverr seller account, completes KYC (CNIC/passport), links Payoneer Revenue Card / bank transfer (PayPal unavailable in PK). Publishes gig manually. 2) Store `FIVERR_API_KEY` in vault via `/api/credentials` (owner only). 3) Verify payout slot. 4) Owner manually accepts orders.

### 3. Freelancer.com (`freelancer`)

- **1. Platform**: Freelancer.com — `https://www.freelancer.com/api/docs` (Official Freelancer API docs: read-only discovery, no automated bidding, payout via PayPal/Payoneer/Wise)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: account/KYC, bidding (human), milestone acceptance, payout — class `freelance_client_work`
- **3. Pakistan availability**: Yes — Pakistan is supported seller country; payout via Payoneer / Wise / bank works in PK (PayPal route unavailable). See Fiverr PK via Payoneer [nsave.com](https://www.nsave.com/pakistan/fiverr) and Upwork PK profiles [upwork.com](https://www.upwork.com/hire/front-end-developers/pk/).
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; platform may request bank/card statement, skills verification (Toptal screening)
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E; platform issues 1099-K only for US persons; PK freelancer self-declares via NTN
- **7. Payout method**: Project → milestone escrow → payout | Platform payout to linked PayPal/Payoneer/Wise / bank; settlement verified via payout-verification adapter + receiving rail proof before entering mission cash; platform fee 5-20% documented. (evidence: Official Freelancer API docs: read-only discovery, no automated bidding, payout via PayPal/Payoneer/Wise) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://www.freelancer.com/api/docs`
- **9. Automation allowed**: NO — Agents may NOT auto-bid or auto-create listings; platform ToS requires human seller identity and owner-approved engagement. Agents may autonomously PERFORM scoped work after owner assigns one agent to one opportunity. (apiPermitted=True, status=ACTIVE, class=freelance_client_work)
- **10. Human-only actions**: account/KYC, bidding (human), milestone acceptance, payout | Class humanControlled: `Owner creates/owns seller account (real identity, KYC, tax), publishes listing (manual), approves scope/price, accepts c`
- **11. Credentials/config needed**: `FREELANCER_OAUTH_TOKEN` in `process.env` OR vault `mission_credentials` provider=`freelancer`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `FREELANCER_OAUTH_TOKEN` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=ACTIVE, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates Freelancer.com account, verifies identity (passport/CNIC, address, bank statement), links Payoneer/Wise payout. 2) Authorize OAuth via `POST /api/freelancer/authorize` (creates `mission_freelancer_accounts` authorized). Store `FREELANCER_OAUTH_TOKEN` in vault. 3) Verify payout slot. Human approves every bid.

### 4. Toptal (`toptal`)

- **1. Platform**: Toptal — `https://www.toptal.com/faq` (Official: screening, client matching via human; no public bidding API)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: screening, profile publish, client matching, contract — class `software_development`
- **3. Pakistan availability**: Verify on officialUrl; Pakistan not in public exclusion list — but PayPal payout not available, use Payoneer/Wise/bank.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; platform may request bank/card statement, skills verification (Toptal screening)
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Toptal matching → client contract → payout | Same as freelance: platform/Stripe payout → payout-verification + bank credit proof. (evidence: Official: screening, client matching via human; no public bidding API) — payoutVerifiable=True
- **8. API/connector available**: NO — no sanctioned bulk automation; manual web UI only — `apiPermitted=False`, `officialUrl=https://www.toptal.com/faq`
- **9. Automation allowed**: YES — Permitted where contract allows AI assistance and human review is disclosed; agents do code work, owner handles repo access and production credentials. (apiPermitted=False, status=PERMITTED, class=software_development)
- **10. Human-only actions**: screening, profile publish, client matching, contract | Class humanControlled: `Owner approves repo/hosting access, credentials via vault, production deployment, contract signing, payout.`
- **11. Credentials/config needed**: `TOPTAL_API_KEY` in `process.env` OR vault `mission_credentials` provider=`toptal`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `TOPTAL_API_KEY` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=False, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain applies to Toptal screening (human interview, portfolio, English). No API bidding — matching is human. 2) Upon acceptance, store `TOPTAL_API_KEY` if provided (read-only). 3) Verify payout slot. All client matching via Toptal human.

### 5. Contra (`contra`)

- **1. Platform**: Contra — `https://help.contra.com/en/articles/9322763` (Official: paid projects escrow 0% client fee, payout via Stripe)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: account/KYC, profile publish, proposal send (human), payout — class `freelance_client_work`
- **3. Pakistan availability**: Yes — Pakistan is supported seller country; payout via Payoneer / Wise / bank works in PK (PayPal route unavailable). See Fiverr PK via Payoneer [nsave.com](https://www.nsave.com/pakistan/fiverr) and Upwork PK profiles [upwork.com](https://www.upwork.com/hire/front-end-developers/pk/).
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; platform may request bank/card statement, skills verification (Toptal screening)
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Project → escrow → payout | Platform payout to linked PayPal/Payoneer/Wise / bank; settlement verified via payout-verification adapter + receiving rail proof before entering mission cash; platform fee 5-20% documented. (evidence: Official: paid projects escrow 0% client fee, payout via Stripe) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://help.contra.com/en/articles/9322763`
- **9. Automation allowed**: NO — Agents may NOT auto-bid or auto-create listings; platform ToS requires human seller identity and owner-approved engagement. Agents may autonomously PERFORM scoped work after owner assigns one agent to one opportunity. (apiPermitted=True, status=ACTIVE, class=freelance_client_work)
- **10. Human-only actions**: account/KYC, profile publish, proposal send (human), payout | Class humanControlled: `Owner creates/owns seller account (real identity, KYC, tax), publishes listing (manual), approves scope/price, accepts c`
- **11. Credentials/config needed**: `CONTRA_API_KEY` in `process.env` OR vault `mission_credentials` provider=`contra`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `CONTRA_API_KEY` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=ACTIVE, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates Contra profile with KYC, links Stripe Connect payout (Stripe Pakistan via Payoneer-bypass? Verify Stripe Connect PK support). 2) Store `CONTRA_API_KEY` in vault. 3) Verify payout slot. Proposal send human.

### 6. PeoplePerHour (`peopleperhour`)

- **1. Platform**: PeoplePerHour — `https://www.peopleperhour.com/fee-schedule` (Official fee schedule, escrow)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: account/KYC, proposal send, payout — class `freelance_client_work`
- **3. Pakistan availability**: Yes — Pakistan is supported seller country; payout via Payoneer / Wise / bank works in PK (PayPal route unavailable). See Fiverr PK via Payoneer [nsave.com](https://www.nsave.com/pakistan/fiverr) and Upwork PK profiles [upwork.com](https://www.upwork.com/hire/front-end-developers/pk/).
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; platform may request bank/card statement, skills verification (Toptal screening)
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Hourlie/Project → escrow → payout | Platform payout to linked PayPal/Payoneer/Wise / bank; settlement verified via payout-verification adapter + receiving rail proof before entering mission cash; platform fee 5-20% documented. (evidence: Official fee schedule, escrow) — payoutVerifiable=True
- **8. API/connector available**: NO — no sanctioned bulk automation; manual web UI only — `apiPermitted=False`, `officialUrl=https://www.peopleperhour.com/fee-schedule`
- **9. Automation allowed**: NO — Agents may NOT auto-bid or auto-create listings; platform ToS requires human seller identity and owner-approved engagement. Agents may autonomously PERFORM scoped work after owner assigns one agent to one opportunity. (apiPermitted=False, status=PERMITTED, class=freelance_client_work)
- **10. Human-only actions**: account/KYC, proposal send, payout | Class humanControlled: `Owner creates/owns seller account (real identity, KYC, tax), publishes listing (manual), approves scope/price, accepts c`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=False, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates PeoplePerHour account with real identity + KYC + Payoneer payout, no public API for bidding. 2) No credential to store (apiPermitted=false) — human performs all proposals. 3) Verify payout slot.

### 7. Guru (`guru`)

- **1. Platform**: Guru — `https://www.guru.com/help/` (Official: SafePay escrow)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: account/KYC, bidding, payout — class `freelance_client_work`
- **3. Pakistan availability**: Yes — Pakistan is supported seller country; payout via Payoneer / Wise / bank works in PK (PayPal route unavailable). See Fiverr PK via Payoneer [nsave.com](https://www.nsave.com/pakistan/fiverr) and Upwork PK profiles [upwork.com](https://www.upwork.com/hire/front-end-developers/pk/).
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; platform may request bank/card statement, skills verification (Toptal screening)
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: SafePay → payout | Platform payout to linked PayPal/Payoneer/Wise / bank; settlement verified via payout-verification adapter + receiving rail proof before entering mission cash; platform fee 5-20% documented. (evidence: Official: SafePay escrow) — payoutVerifiable=True
- **8. API/connector available**: NO — no sanctioned bulk automation; manual web UI only — `apiPermitted=False`, `officialUrl=https://www.guru.com/help/`
- **9. Automation allowed**: NO — Agents may NOT auto-bid or auto-create listings; platform ToS requires human seller identity and owner-approved engagement. Agents may autonomously PERFORM scoped work after owner assigns one agent to one opportunity. (apiPermitted=False, status=PERMITTED, class=freelance_client_work)
- **10. Human-only actions**: account/KYC, bidding, payout | Class humanControlled: `Owner creates/owns seller account (real identity, KYC, tax), publishes listing (manual), approves scope/price, accepts c`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=False, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates Guru account with real identity + KYC + Payoneer payout, no public API for bidding. 2) No credential to store (apiPermitted=false) — human performs all proposals. 3) Verify payout slot.

### 8. HackerOne (`hackerone`)

- **1. Platform**: HackerOne — `https://docs.hackerone.com/programs/philosophy` (Official: in-scope authorized testing only, bounty $50-10k+, payout via PayPal/bank; API for program info read-only)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: scope/authorization verification, target approval, report submission (human), disclosure coordination, payout/KYC — class `bug_bounties`
- **3. Pakistan availability**: Yes — global, but bounty payout via PayPal/bank; in PK use bank wire (PayPal not available). Verify program eligibility on officialUrl.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; program-specific scope agreement, competition team identity
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Valid report → bounty payout | HackerOne/Bugcrowd payout statement + bank credit; verified before cash. (evidence: Official: in-scope authorized testing only, bounty $50-10k+, payout via PayPal/bank; API for program info read-only) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://docs.hackerone.com/programs/philosophy`
- **9. Automation allowed**: YES — Permitted ONLY on in-scope, authorized targets with explicit permission; disclosure and submission are human-controlled. (apiPermitted=True, status=ACTIVE, class=bug_bounties)
- **10. Human-only actions**: scope/authorization verification, target approval, report submission (human), disclosure coordination, payout/KYC | Class humanControlled: `Owner verifies scope/authorization, approves target, submits report manually, handles payout/KYC, coordinates disclosure`
- **11. Credentials/config needed**: `HACKERONE_API_KEY` in `process.env` OR vault `mission_credentials` provider=`hackerone`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `HACKERONE_API_KEY` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=ACTIVE, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates HackerOne researcher account, completes KYC/tax, links bank/PayPal (use bank wire — PayPal not in PK). 2) Store `HACKERONE_API_KEY` in vault via `/api/credentials` (read-only program API). 3) Human must verify every program scope before agent touches target (`humanOnlyActions: scope verification, target approval, report submission`). 4) Verify payout slot.

### 9. Bugcrowd (`bugcrowd`)

- **1. Platform**: Bugcrowd — `https://www.bugcrowd.com/resource/what-is-bug-bounty/` (Official bounty terms, payout via PayPal/bank)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: scope verification, submission (human), payout — class `bug_bounties`
- **3. Pakistan availability**: Yes — global, but bounty payout via PayPal/bank; in PK use bank wire (PayPal not available). Verify program eligibility on officialUrl.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; program-specific scope agreement, competition team identity
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Valid report → bounty payout | HackerOne/Bugcrowd payout statement + bank credit; verified before cash. (evidence: Official bounty terms, payout via PayPal/bank) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://www.bugcrowd.com/resource/what-is-bug-bounty/`
- **9. Automation allowed**: YES — Permitted ONLY on in-scope, authorized targets with explicit permission; disclosure and submission are human-controlled. (apiPermitted=True, status=ACTIVE, class=bug_bounties)
- **10. Human-only actions**: scope verification, submission (human), payout | Class humanControlled: `Owner verifies scope/authorization, approves target, submits report manually, handles payout/KYC, coordinates disclosure`
- **11. Credentials/config needed**: `BUGCROWD_API_KEY` in `process.env` OR vault `mission_credentials` provider=`bugcrowd`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `BUGCROWD_API_KEY` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=ACTIVE, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates Bugcrowd researcher account, completes KYC/tax, links bank/PayPal (use bank wire — PayPal not in PK). 2) Store `BUGCROWD_API_KEY` in vault via `/api/credentials` (read-only program API). 3) Human must verify every program scope before agent touches target (`humanOnlyActions: scope verification, target approval, report submission`). 4) Verify payout slot.

### 10. YesWeHack (`yeswehack`)

- **1. Platform**: YesWeHack — `https://www.yeswehack.com/` (Directory: bounty programs)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: scope verification, submission, payout — class `bug_bounties`
- **3. Pakistan availability**: Yes — global, but bounty payout via PayPal/bank; in PK use bank wire (PayPal not available). Verify program eligibility on officialUrl.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; program-specific scope agreement, competition team identity
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Bounty payout | HackerOne/Bugcrowd payout statement + bank credit; verified before cash. (evidence: Directory: bounty programs) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://www.yeswehack.com/`
- **9. Automation allowed**: YES — Permitted ONLY on in-scope, authorized targets with explicit permission; disclosure and submission are human-controlled. (apiPermitted=True, status=PERMITTED, class=bug_bounties)
- **10. Human-only actions**: scope verification, submission, payout | Class humanControlled: `Owner verifies scope/authorization, approves target, submits report manually, handles payout/KYC, coordinates disclosure`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates YesWeHack researcher account, completes KYC/tax, links bank/PayPal (use bank wire — PayPal not in PK). 2) Store `` in vault via `/api/credentials` (read-only program API). 3) Human must verify every program scope before agent touches target (`humanOnlyActions: scope verification, target approval, report submission`). 4) Verify payout slot.

### 11. Intigriti (`intigriti`)

- **1. Platform**: Intigriti — `https://www.intigriti.com/` (Directory: bounty programs)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: scope verification, submission, payout — class `bug_bounties`
- **3. Pakistan availability**: Yes — global, but bounty payout via PayPal/bank; in PK use bank wire (PayPal not available). Verify program eligibility on officialUrl.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; program-specific scope agreement, competition team identity
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Bounty payout | HackerOne/Bugcrowd payout statement + bank credit; verified before cash. (evidence: Directory: bounty programs) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://www.intigriti.com/`
- **9. Automation allowed**: YES — Permitted ONLY on in-scope, authorized targets with explicit permission; disclosure and submission are human-controlled. (apiPermitted=True, status=PERMITTED, class=bug_bounties)
- **10. Human-only actions**: scope verification, submission, payout | Class humanControlled: `Owner verifies scope/authorization, approves target, submits report manually, handles payout/KYC, coordinates disclosure`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates Intigriti researcher account, completes KYC/tax, links bank/PayPal (use bank wire — PayPal not in PK). 2) Store `` in vault via `/api/credentials` (read-only program API). 3) Human must verify every program scope before agent touches target (`humanOnlyActions: scope verification, target approval, report submission`). 4) Verify payout slot.

### 12. Kaggle Competitions (`kaggle`)

- **1. Platform**: Kaggle Competitions — `https://www.kaggle.com/competitions` (Official: prize pools $10k-100k, payout via bank/PayPal, AI assistance allowed where rules permit)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: eligibility verification, rules on AI, team identity, submission (human), KYC/tax — class `contests_challenges`
- **3. Pakistan availability**: Yes — global contest/QA; prize/payout via bank/PayPal; PK eligible, but use bank/Payoneer, not PayPal.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; program-specific scope agreement, competition team identity
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Winning submission → prize payout | Organizer prize statement + bank credit; verified; prize not guaranteed. (evidence: Official: prize pools $10k-100k, payout via bank/PayPal, AI assistance allowed where rules permit) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://www.kaggle.com/competitions`
- **9. Automation allowed**: YES — Permitted where rules allow AI assistance; submission and team identity are human-controlled. (apiPermitted=True, status=ACTIVE, class=contests_challenges)
- **10. Human-only actions**: eligibility verification, rules on AI, team identity, submission (human), KYC/tax | Class humanControlled: `Owner verifies eligibility, rules on AI use, submits, handles KYC/tax for prize.`
- **11. Credentials/config needed**: `KAGGLE_API_KEY` in `process.env` OR vault `mission_credentials` provider=`kaggle`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `KAGGLE_API_KEY` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=ACTIVE, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates Kaggle Competitions account, verifies eligibility, confirms AI-rules for competition (some ban AI). 2) Store `KAGGLE_API_KEY` if Kaggle. 3) Human submits entry (cannot automate final submission). 4) Verify payout slot + provide KYC/tax for prize.

### 13. Topcoder (`topcoder`)

- **1. Platform**: Topcoder — `https://www.topcoder.com/thrive/articles/Topcoder%20Platform` (Directory: contests/prize)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: submission (human), payout — class `contests_challenges`
- **3. Pakistan availability**: Yes — global contest/QA; prize/payout via bank/PayPal; PK eligible, but use bank/Payoneer, not PayPal.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; program-specific scope agreement, competition team identity
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Contest prize payout | Organizer prize statement + bank credit; verified; prize not guaranteed. (evidence: Directory: contests/prize) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://www.topcoder.com/thrive/articles/Topcoder%20Platform`
- **9. Automation allowed**: YES — Permitted where rules allow AI assistance; submission and team identity are human-controlled. (apiPermitted=True, status=PERMITTED, class=contests_challenges)
- **10. Human-only actions**: submission (human), payout | Class humanControlled: `Owner verifies eligibility, rules on AI use, submits, handles KYC/tax for prize.`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates Topcoder account, verifies eligibility, confirms AI-rules for competition (some ban AI). 2) Store `KAGGLE_API_KEY` if Kaggle. 3) Human submits entry (cannot automate final submission). 4) Verify payout slot + provide KYC/tax for prize.

### 14. Devpost (`devpost`)

- **1. Platform**: Devpost — `https://devpost.com/rules` (Official rules)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: submission, payout — class `contests_challenges`
- **3. Pakistan availability**: Yes — global contest/QA; prize/payout via bank/PayPal; PK eligible, but use bank/Payoneer, not PayPal.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Hackathon prize | Organizer prize statement + bank credit; verified; prize not guaranteed. (evidence: Official rules) — payoutVerifiable=True
- **8. API/connector available**: NO — no sanctioned bulk automation; manual web UI only — `apiPermitted=False`, `officialUrl=https://devpost.com/rules`
- **9. Automation allowed**: YES — Permitted where rules allow AI assistance; submission and team identity are human-controlled. (apiPermitted=False, status=PERMITTED, class=contests_challenges)
- **10. Human-only actions**: submission, payout | Class humanControlled: `Owner verifies eligibility, rules on AI use, submits, handles KYC/tax for prize.`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=False, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates Devpost account, verifies eligibility, confirms AI-rules for competition (some ban AI). 2) Store `KAGGLE_API_KEY` if Kaggle. 3) Human submits entry (cannot automate final submission). 4) Verify payout slot + provide KYC/tax for prize.

### 15. RapidAPI (`rapidapi`)

- **1. Platform**: RapidAPI — `https://rapidapi.com/developer/billing` (Official: provider payouts 80% via PayPal/bank)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: data rights verification, publisher account/KYC, publishing, support/payout — class `paid_apis_data`
- **3. Pakistan availability**: Available in PK if platform supports PK payout country; most via PayPal/bank/Payoneer — for PK use Payoneer/Wise/bank (PayPal not available). Confirm on officialUrl payout section.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; data-rights declaration, store tax form (W-8BEN/W-9)
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: API subscription → marketplace payout | Marketplace payout → bank; verified. (evidence: Official: provider payouts 80% via PayPal/bank) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://rapidapi.com/developer/billing`
- **9. Automation allowed**: NO — Building pipeline may be automated, but data licensing, publishing, and compliance are human. (apiPermitted=True, status=PERMITTED, class=paid_apis_data)
- **10. Human-only actions**: data rights verification, publisher account/KYC, publishing, support/payout | Class humanControlled: `Owner verifies data rights, licenses, publishes, handles support/payout.`
- **11. Credentials/config needed**: `RAPIDAPI_KEY` in `process.env` OR vault `mission_credentials` provider=`rapidapi`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `RAPIDAPI_KEY` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain verifies data-rights ownership (you own the data). 2) Creates RapidAPI publisher account with KYC + bank/PayPal (use Payoneer/Wise; PayPal not available). 3) Store `RAPIDAPI_KEY` in vault. 4) Publish API/dataset manually (human). 5) Verify payout slot.

### 16. AWS Data Exchange (`aws_data_exchange`)

- **1. Platform**: AWS Data Exchange — `https://aws.amazon.com/data-exchange/` (Official: data provider payouts)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: data rights, publishing, payout — class `paid_apis_data`
- **3. Pakistan availability**: Available in PK if platform supports PK payout country; most via PayPal/bank/Payoneer — for PK use Payoneer/Wise/bank (PayPal not available). Confirm on officialUrl payout section.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; data-rights declaration, store tax form (W-8BEN/W-9)
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Data subscription → payout | Marketplace payout → bank; verified. (evidence: Official: data provider payouts) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://aws.amazon.com/data-exchange/`
- **9. Automation allowed**: NO — Building pipeline may be automated, but data licensing, publishing, and compliance are human. (apiPermitted=True, status=PERMITTED, class=paid_apis_data)
- **10. Human-only actions**: data rights, publishing, payout | Class humanControlled: `Owner verifies data rights, licenses, publishes, handles support/payout.`
- **11. Credentials/config needed**: `AWS_DATA_EXCHANGE_TOKEN` in `process.env` OR vault `mission_credentials` provider=`aws_data_exchange`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `AWS_DATA_EXCHANGE_TOKEN` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain verifies data-rights ownership (you own the data). 2) Creates AWS Data Exchange publisher account with KYC + bank/PayPal (use Payoneer/Wise; PayPal not available). 3) Store `AWS_DATA_EXCHANGE_TOKEN` in vault. 4) Publish API/dataset manually (human). 5) Verify payout slot.

### 17. Gumroad (`gumroad`)

- **1. Platform**: Gumroad — `https://gumroad.com/help/article/13-getting-paid` (Official: payouts PayPal/bank fees 10%)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: IP verification, listing publish (manual), refunds/support, payout — class `digital_products`
- **3. Pakistan availability**: Available in PK if platform supports PK payout country; most via PayPal/bank/Payoneer — for PK use Payoneer/Wise/bank (PayPal not available). Confirm on officialUrl payout section.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; data-rights declaration, store tax form (W-8BEN/W-9)
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E; MoR (Merchant of Record) handles VAT/GST; seller provides tax ID for payouts
- **7. Payout method**: Digital sale → MoR payout | Gumroad/Lemon Squeezy payout after fees (Gumroad 10% + Stripe); bank credit verified. (evidence: Official: payouts PayPal/bank fees 10%) — payoutVerifiable=True
- **8. API/connector available**: NO — no sanctioned bulk automation; manual web UI only — `apiPermitted=False`, `officialUrl=https://gumroad.com/help/article/13-getting-paid`
- **9. Automation allowed**: NO — Product preparation may be automated, but IP clearance, listing, and support are human. (apiPermitted=False, status=PERMITTED, class=digital_products)
- **10. Human-only actions**: IP verification, listing publish (manual), refunds/support, payout | Class humanControlled: `Owner verifies IP, lists manually (Gumroad Discover requires actual sales + review), handles refunds/support.`
- **11. Credentials/config needed**: `GUMROAD_API_KEY` in `process.env` OR vault `mission_credentials` provider=`gumroad`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `GUMROAD_API_KEY` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=False, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates Gumroad store with KYC + PayPal/bank (use Payoneer/Wise; Gumroad docs note PayPal/bank payouts). Confirms own IP rights. 2) Publishes listing manually — no auto-publish. 3) Store `GUMROAD_API_KEY` if API exists (currently `apiPermitted=false` so vault not required). 4) Verify payout slot. Handles refunds/support human.

### 18. Lemon Squeezy (`lemon_squeezy`)

- **1. Platform**: Lemon Squeezy — `https://docs.lemonsqueezy.com/help/getting-started/payouts` (Official payouts via Stripe Connect)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: IP verification, listing, payout — class `digital_products`
- **3. Pakistan availability**: Available in PK if platform supports PK payout country; most via PayPal/bank/Payoneer — for PK use Payoneer/Wise/bank (PayPal not available). Confirm on officialUrl payout section.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; data-rights declaration, store tax form (W-8BEN/W-9)
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E; MoR (Merchant of Record) handles VAT/GST; seller provides tax ID for payouts
- **7. Payout method**: Digital sale → payout | Gumroad/Lemon Squeezy payout after fees (Gumroad 10% + Stripe); bank credit verified. (evidence: Official payouts via Stripe Connect) — payoutVerifiable=True
- **8. API/connector available**: NO — no sanctioned bulk automation; manual web UI only — `apiPermitted=False`, `officialUrl=https://docs.lemonsqueezy.com/help/getting-started/payouts`
- **9. Automation allowed**: NO — Product preparation may be automated, but IP clearance, listing, and support are human. (apiPermitted=False, status=PERMITTED, class=digital_products)
- **10. Human-only actions**: IP verification, listing, payout | Class humanControlled: `Owner verifies IP, lists manually (Gumroad Discover requires actual sales + review), handles refunds/support.`
- **11. Credentials/config needed**: `LEMON_SQUEEZY_API_KEY` in `process.env` OR vault `mission_credentials` provider=`lemon_squeezy`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `LEMON_SQUEEZY_API_KEY` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=False, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates Lemon Squeezy store with KYC + PayPal/bank (use Payoneer/Wise; Gumroad docs note PayPal/bank payouts). Confirms own IP rights. 2) Publishes listing manually — no auto-publish. 3) Store `LEMON_SQUEEZY_API_KEY` if API exists (currently `apiPermitted=false` so vault not required). 4) Verify payout slot. Handles refunds/support human.

### 19. Etsy (digital) (`etsy_digital`)

- **1. Platform**: Etsy (digital) — `https://www.etsy.com/help/article/479` (Official seller payouts)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: listing, payout — class `digital_products`
- **3. Pakistan availability**: Available in PK if platform supports PK payout country; most via PayPal/bank/Payoneer — for PK use Payoneer/Wise/bank (PayPal not available). Confirm on officialUrl payout section.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; data-rights declaration, store tax form (W-8BEN/W-9)
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E; MoR (Merchant of Record) handles VAT/GST; seller provides tax ID for payouts
- **7. Payout method**: Digital sale → payout | Gumroad/Lemon Squeezy payout after fees (Gumroad 10% + Stripe); bank credit verified. (evidence: Official seller payouts) — payoutVerifiable=True
- **8. API/connector available**: NO — no sanctioned bulk automation; manual web UI only — `apiPermitted=False`, `officialUrl=https://www.etsy.com/help/article/479`
- **9. Automation allowed**: NO — Product preparation may be automated, but IP clearance, listing, and support are human. (apiPermitted=False, status=PERMITTED, class=digital_products)
- **10. Human-only actions**: listing, payout | Class humanControlled: `Owner verifies IP, lists manually (Gumroad Discover requires actual sales + review), handles refunds/support.`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=False, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates Etsy (digital) store with KYC + PayPal/bank (use Payoneer/Wise; Gumroad docs note PayPal/bank payouts). Confirms own IP rights. 2) Publishes listing manually — no auto-publish. 3) Store `` if API exists (currently `apiPermitted=false` so vault not required). 4) Verify payout slot. Handles refunds/support human.

### 20. Shopify App Store (`shopify_app_store`)

- **1. Platform**: Shopify App Store — `https://shopify.dev/docs/apps/launch/app-store-review` (Official: app review, revenue share 80/20)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: developer account, agreements, submission for review (human), support/payout — class `app_marketplaces`
- **3. Pakistan availability**: Available in PK if platform supports PK payout country; most via PayPal/bank/Payoneer — for PK use Payoneer/Wise/bank (PayPal not available). Confirm on officialUrl payout section.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E; MoR (Merchant of Record) handles VAT/GST; seller provides tax ID for payouts
- **7. Payout method**: App subscription → platform payout | Platform payout via PayPal/bank; verified. (evidence: Official: app review, revenue share 80/20) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://shopify.dev/docs/apps/launch/app-store-review`
- **9. Automation allowed**: NO — Code assistance permitted, but marketplace review, publishing, and support are human. (apiPermitted=True, status=PERMITTED, class=app_marketplaces)
- **10. Human-only actions**: developer account, agreements, submission for review (human), support/payout | Class humanControlled: `Owner holds developer account, signs agreements, submits, handles support/payout.`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates developer account (Shopify Partners / Atlassian Developer) with KYC/tax, agrees terms, links payout (Stripe Connect/ PayPal — use Stripe where PK supported). 2) Human submits app for review (cannot automate review). 3) Store credential if API permitted. 4) Verify payout slot.

### 21. Atlassian Marketplace (`atlassian_marketplace`)

- **1. Platform**: Atlassian Marketplace — `https://developer.atlassian.com/platform/marketplace/` (Official: revenue share 85/15)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: developer account, submission, payout — class `app_marketplaces`
- **3. Pakistan availability**: Available in PK if platform supports PK payout country; most via PayPal/bank/Payoneer — for PK use Payoneer/Wise/bank (PayPal not available). Confirm on officialUrl payout section.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: App sale → payout | Platform payout via PayPal/bank; verified. (evidence: Official: revenue share 85/15) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://developer.atlassian.com/platform/marketplace/`
- **9. Automation allowed**: NO — Code assistance permitted, but marketplace review, publishing, and support are human. (apiPermitted=True, status=PERMITTED, class=app_marketplaces)
- **10. Human-only actions**: developer account, submission, payout | Class humanControlled: `Owner holds developer account, signs agreements, submits, handles support/payout.`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates developer account (Shopify Partners / Atlassian Developer) with KYC/tax, agrees terms, links payout (Stripe Connect/ PayPal — use Stripe where PK supported). 2) Human submits app for review (cannot automate review). 3) Store credential if API permitted. 4) Verify payout slot.

### 22. WordPress Plugin Directory (`wordpress_plugin`)

- **1. Platform**: WordPress Plugin Directory — `https://developer.wordpress.org/plugins/` (Directory: plugin sales via Freemius/EDD)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: submission, payout — class `app_marketplaces`
- **3. Pakistan availability**: Available in PK if platform supports PK payout country; most via PayPal/bank/Payoneer — for PK use Payoneer/Wise/bank (PayPal not available). Confirm on officialUrl payout section.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Plugin sale → payout | Platform payout via PayPal/bank; verified. (evidence: Directory: plugin sales via Freemius/EDD) — payoutVerifiable=True
- **8. API/connector available**: NO — no sanctioned bulk automation; manual web UI only — `apiPermitted=False`, `officialUrl=https://developer.wordpress.org/plugins/`
- **9. Automation allowed**: NO — Code assistance permitted, but marketplace review, publishing, and support are human. (apiPermitted=False, status=PERMITTED, class=app_marketplaces)
- **10. Human-only actions**: submission, payout | Class humanControlled: `Owner holds developer account, signs agreements, submits, handles support/payout.`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=False, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates developer account (Shopify Partners / Atlassian Developer) with KYC/tax, agrees terms, links payout (Stripe Connect/ PayPal — use Stripe where PK supported). 2) Human submits app for review (cannot automate review). 3) Store credential if API permitted. 4) Verify payout slot.

### 23. GitHub Sponsors (`github_sponsors`)

- **1. Platform**: GitHub Sponsors — `https://docs.github.com/en/sponsors` (Official: payout via Stripe Connect, no auto-solicitation)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: repo ownership, Sponsors enrollment (KYC), sponsor relationship, payout — class `open_source_sponsorship`
- **3. Pakistan availability**: Available in PK if platform supports PK payout country; most via PayPal/bank/Payoneer — for PK use Payoneer/Wise/bank (PayPal not available). Confirm on officialUrl payout section.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; instructor/collective verification, payout tax form
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Sponsor → Stripe payout | GitHub Sponsors payout statement + Stripe bank credit; verified. (evidence: Official: payout via Stripe Connect, no auto-solicitation) — payoutVerifiable=True
- **8. API/connector available**: NO — no sanctioned bulk automation; manual web UI only — `apiPermitted=False`, `officialUrl=https://docs.github.com/en/sponsors`
- **9. Automation allowed**: NO — Code work may be automated, but sponsorship solicitation and sponsor relationship are human; no fake stars. (apiPermitted=False, status=PERMITTED, class=open_source_sponsorship)
- **10. Human-only actions**: repo ownership, Sponsors enrollment (KYC), sponsor relationship, payout | Class humanControlled: `Owner owns repo, builds reputation, enrolls in Sponsors (KYC), engages sponsors.`
- **11. Credentials/config needed**: `GITHUB_TOKEN` in `process.env` OR vault `mission_credentials` provider=`github_sponsors`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `GITHUB_TOKEN` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=False, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain owns GitHub repo/org, enrolls in GitHub Sponsors (Stripe KYC) or Open Collective collective, links Stripe payout (Stripe in PK via Atlas/payoneer alternative — verify Stripe PK). 2) Store `GITHUB_TOKEN` in vault for read-only sponsorship read. 3) Verify payout slot. Sponsor relationship is human.

### 24. Open Collective (`open_collective`)

- **1. Platform**: Open Collective — `https://docs.opencollective.com/help/collectives` (Official: payouts)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: collective ownership, payout — class `open_source_sponsorship`
- **3. Pakistan availability**: Available in PK if platform supports PK payout country; most via PayPal/bank/Payoneer — for PK use Payoneer/Wise/bank (PayPal not available). Confirm on officialUrl payout section.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; instructor/collective verification, payout tax form
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Donation → payout | GitHub Sponsors payout statement + Stripe bank credit; verified. (evidence: Official: payouts) — payoutVerifiable=True
- **8. API/connector available**: NO — no sanctioned bulk automation; manual web UI only — `apiPermitted=False`, `officialUrl=https://docs.opencollective.com/help/collectives`
- **9. Automation allowed**: NO — Code work may be automated, but sponsorship solicitation and sponsor relationship are human; no fake stars. (apiPermitted=False, status=PERMITTED, class=open_source_sponsorship)
- **10. Human-only actions**: collective ownership, payout | Class humanControlled: `Owner owns repo, builds reputation, enrolls in Sponsors (KYC), engages sponsors.`
- **11. Credentials/config needed**: `OPENCOLLECTIVE_API_KEY` in `process.env` OR vault `mission_credentials` provider=`open_collective`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `OPENCOLLECTIVE_API_KEY` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=False, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain owns GitHub repo/org, enrolls in GitHub Sponsors (Stripe KYC) or Open Collective collective, links Stripe payout (Stripe in PK via Atlas/payoneer alternative — verify Stripe PK). 2) Store `GITHUB_TOKEN` in vault for read-only sponsorship read. 3) Verify payout slot. Sponsor relationship is human.

### 25. Udemy (`udemy`)

- **1. Platform**: Udemy — `https://support.udemy.com/hc/en-us/articles/229604988` (Official: revenue share 37% default, 97% via promo, payout via PayPal/Payoneer; publishing requires human)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: instructor account/tax/payout, course publish (manual), learner data, payout — class `tutoring_education`
- **3. Pakistan availability**: Available in PK with KYC; verify on officialUrl. PayPal not usable — use Payoneer/bank where offered.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; instructor/collective verification, payout tax form
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Course sale → payout | Platform payout (Udemy revenue share 37-97%, Preply via PayPal/Payoneer); verified before cash. (evidence: Official: revenue share 37% default, 97% via promo, payout via PayPal/Payoneer; publishing requires human) — payoutVerifiable=True
- **8. API/connector available**: NO — no sanctioned bulk automation; manual web UI only — `apiPermitted=False`, `officialUrl=https://support.udemy.com/hc/en-us/articles/229604988`
- **9. Automation allowed**: NO — Content generation permitted; live tutoring/credentialed teaching requires qualified human and cannot be automated. (apiPermitted=False, status=RESTRICTED, class=tutoring_education)
- **10. Human-only actions**: instructor account/tax/payout, course publish (manual), learner data, payout | Class humanControlled: `Owner verifies qualifications, publishes course (manual), handles learner data, receives payout.`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: `status=RESTRICTED` — requires additional owner setup / restricted per ToS `Official: revenue share 37% default, 97% via promo, payout via PayPal/Payoneer; publishing requires human`; plus no credential + payout slot unconfigured.
- **13. Current readiness status**: `restricted` (kind=EARNING_SOURCE, status=RESTRICTED, api=False, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates instructor/tutor account, completes tax (W-8BEN) + KYC (passport/CNIC, payout via PayPal/Payoneer — use Payoneer). 2) Publishes course/listing manually. 3) For Preply live tutoring is `RESTRICTED_HUMAN_ONLY` — human must teach. 4) Verify payout slot.

### 26. Preply (`preply`)

- **1. Platform**: Preply — `https://preply.com/en/question/how-do-tutors-get-paid-40260` (Official: tutor payouts PayPal/Payoneer)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: tutor account/KYC, live tutoring (human), payout — class `tutoring_education`
- **3. Pakistan availability**: Available in PK with KYC; verify on officialUrl. PayPal not usable — use Payoneer/bank where offered.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID; instructor/collective verification, payout tax form
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Lesson → payout | Platform payout (Udemy revenue share 37-97%, Preply via PayPal/Payoneer); verified before cash. (evidence: Official: tutor payouts PayPal/Payoneer) — payoutVerifiable=True
- **8. API/connector available**: NO — no sanctioned bulk automation; manual web UI only — `apiPermitted=False`, `officialUrl=https://preply.com/en/question/how-do-tutors-get-paid-40260`
- **9. Automation allowed**: NO — Content generation permitted; live tutoring/credentialed teaching requires qualified human and cannot be automated. (apiPermitted=False, status=RESTRICTED, class=tutoring_education)
- **10. Human-only actions**: tutor account/KYC, live tutoring (human), payout | Class humanControlled: `Owner verifies qualifications, publishes course (manual), handles learner data, receives payout.`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: `status=RESTRICTED` — requires additional owner setup / restricted per ToS `Official: tutor payouts PayPal/Payoneer`; plus no credential + payout slot unconfigured.
- **13. Current readiness status**: `restricted` (kind=EARNING_SOURCE, status=RESTRICTED, api=False, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates instructor/tutor account, completes tax (W-8BEN) + KYC (passport/CNIC, payout via PayPal/Payoneer — use Payoneer). 2) Publishes course/listing manually. 3) For Preply live tutoring is `RESTRICTED_HUMAN_ONLY` — human must teach. 4) Verify payout slot.

### 27. Amazon Associates (`amazon_associates`)

- **1. Platform**: Amazon Associates — `https://affiliate-program.amazon.com/help/node/topic/GRXPHT8U84RAYDXZ` (Official fee schedule 0-10%)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: enrollment (real identity/website), disclosure, link placement (manual), tax, payout — class `affiliate_referral`
- **3. Pakistan availability**: Available in PK if platform supports PK payout country; most via PayPal/bank/Payoneer — for PK use Payoneer/Wise/bank (PayPal not available). Confirm on officialUrl payout section.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E; Amazon/Awin require site disclosure + operational site + tax interview (W-8BEN, 30% withholding without treaty)
- **7. Payout method**: Referred sale → commission payout | Network statement → bank credit; Amazon fee schedule 0-10%, Awin via SEPA/BACS/ACH; verified before cash. (evidence: Official fee schedule 0-10%) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://affiliate-program.amazon.com/help/node/topic/GRXPHT8U84RAYDXZ`
- **9. Automation allowed**: NO — Content creation may be automated, but program enrollment, link issuance, and traffic generation require owner identity and manual disclosure; fake engagement is prohibited. (apiPermitted=True, status=PERMITTED, class=affiliate_referral)
- **10. Human-only actions**: enrollment (real identity/website), disclosure, link placement (manual), tax, payout | Class humanControlled: `Owner enrolls (real identity, website/social), discloses, places links manually, handles tax.`
- **11. Credentials/config needed**: `AMAZON_ASSOCIATE_TAG` in `process.env` OR vault `mission_credentials` provider=`amazon_associates`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `AMAZON_ASSOCIATE_TAG` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain enrolls at Amazon Associates with real website, completes site review, adds disclosure (FTC), completes tax interview (W-8BEN, 30% US withholding without treaty). Must have operational site. 2) Store `AMAZON_ASSOCIATE_TAG` in vault (Awin/ShareASale API, Amazon tag). 3) Link placement is manual human (affiliate rules). 4) Verify payout slot. Check Pakistan program eligibility (Amazon.pk limited — verify on https://affiliate-program.amazon.com/help/node/topic/GRXPHT8U84RAYDXZ).

### 28. Awin (`awin`)

- **1. Platform**: Awin — `https://www.awin.com/us/publishers` (Official: 1M+ publishers, SEPA/ACH/Payoneer)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: enrollment, disclosure, link placement, payout — class `affiliate_referral`
- **3. Pakistan availability**: Available in PK if platform supports PK payout country; most via PayPal/bank/Payoneer — for PK use Payoneer/Wise/bank (PayPal not available). Confirm on officialUrl payout section.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E; Amazon/Awin require site disclosure + operational site + tax interview (W-8BEN, 30% withholding without treaty)
- **7. Payout method**: Referred sale → network payout | Network statement → bank credit; Amazon fee schedule 0-10%, Awin via SEPA/BACS/ACH; verified before cash. (evidence: Official: 1M+ publishers, SEPA/ACH/Payoneer) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://www.awin.com/us/publishers`
- **9. Automation allowed**: NO — Content creation may be automated, but program enrollment, link issuance, and traffic generation require owner identity and manual disclosure; fake engagement is prohibited. (apiPermitted=True, status=ACTIVE, class=affiliate_referral)
- **10. Human-only actions**: enrollment, disclosure, link placement, payout | Class humanControlled: `Owner enrolls (real identity, website/social), discloses, places links manually, handles tax.`
- **11. Credentials/config needed**: `AWIN_API_KEY` in `process.env` OR vault `mission_credentials` provider=`awin`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `AWIN_API_KEY` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=ACTIVE, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain enrolls at Awin with real website, completes site review, adds disclosure (FTC), completes tax interview (W-8BEN, 30% US withholding without treaty). Must have operational site. 2) Store `AWIN_API_KEY` in vault (Awin/ShareASale API, Amazon tag). 3) Link placement is manual human (affiliate rules). 4) Verify payout slot. Check Pakistan program eligibility (Amazon.pk limited — verify on https://www.awin.com/us/publishers).

### 29. ShareASale (`shareasale`)

- **1. Platform**: ShareASale — `https://www.shareasale.com/info/` (Official affiliate network)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: enrollment, disclosure, payout — class `affiliate_referral`
- **3. Pakistan availability**: Available in PK if platform supports PK payout country; most via PayPal/bank/Payoneer — for PK use Payoneer/Wise/bank (PayPal not available). Confirm on officialUrl payout section.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E; Amazon/Awin require site disclosure + operational site + tax interview (W-8BEN, 30% withholding without treaty)
- **7. Payout method**: Commission payout | Network statement → bank credit; Amazon fee schedule 0-10%, Awin via SEPA/BACS/ACH; verified before cash. (evidence: Official affiliate network) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://www.shareasale.com/info/`
- **9. Automation allowed**: NO — Content creation may be automated, but program enrollment, link issuance, and traffic generation require owner identity and manual disclosure; fake engagement is prohibited. (apiPermitted=True, status=PERMITTED, class=affiliate_referral)
- **10. Human-only actions**: enrollment, disclosure, payout | Class humanControlled: `Owner enrolls (real identity, website/social), discloses, places links manually, handles tax.`
- **11. Credentials/config needed**: `SHAREASALE_API_KEY` in `process.env` OR vault `mission_credentials` provider=`shareasale`; stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `SHAREASALE_API_KEY` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain enrolls at ShareASale with real website, completes site review, adds disclosure (FTC), completes tax interview (W-8BEN, 30% US withholding without treaty). Must have operational site. 2) Store `SHAREASALE_API_KEY` in vault (Awin/ShareASale API, Amazon tag). 3) Link placement is manual human (affiliate rules). 4) Verify payout slot. Check Pakistan program eligibility (Amazon.pk limited — verify on https://www.shareasale.com/info/).

### 30. Test.io (`testio`)

- **1. Platform**: Test.io — `https://test.io/become-a-tester/` (Directory: QA payouts PayPal/Payoneer; ToS allows human tester with tool assistance for static checks)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: tester account, target authorization (human), disclosure (human), payout — class `qa_testing`
- **3. Pakistan availability**: Yes — global contest/QA; prize/payout via bank/PayPal; PK eligible, but use bank/Payoneer, not PayPal.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Bug repro → payout | PayPal/Payoneer/bank via platform; verified before treasury (same chain). (evidence: Directory: QA payouts PayPal/Payoneer; ToS allows human tester with tool assistance for static checks) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://test.io/become-a-tester/`
- **9. Automation allowed**: YES — Static offline checks are permitted; any crawling/fetching requires owner-authorized HTML source, not URL; no pen-testing. (apiPermitted=True, status=PERMITTED, class=qa_testing)
- **10. Human-only actions**: tester account, target authorization (human), disclosure (human), payout | Class humanControlled: `Owner authorizes target (HTML source, not URL), reviews checklist, approves disclosure, handles platform account.`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=True, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates Test.io tester/translator account with language verification + KYC, links PayPal/bank (use Payoneer). 2) Human must authorize target & disclosure. 3) No credential (apiPermitted true for Test.io) — store if available. 4) Verify payout slot.

### 31. Gengo (`gengo`)

- **1. Platform**: Gengo — `https://gengo.com/translators/` (Official translator payouts)
- **2. Account required**: Yes — `requiresOwnerAccount=true`, human actions: translator account, language verification, payout — class `writing_translation`
- **3. Pakistan availability**: Yes — global contest/QA; prize/payout via bank/PayPal; PK eligible, but use bank/Payoneer, not PayPal.
- **4. KYC/identity required**: Yes — real human identity, 18+, government verification
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Translation → payout | Payout-verification + receiving rail; same net USD gate. (evidence: Official translator payouts) — payoutVerifiable=True
- **8. API/connector available**: NO — no sanctioned bulk automation; manual web UI only — `apiPermitted=False`, `officialUrl=https://gengo.com/translators/`
- **9. Automation allowed**: YES — Drafting is permitted where contract allows AI assistance; final publication and client communication remain human-reviewed. (apiPermitted=False, status=PERMITTED, class=writing_translation)
- **10. Human-only actions**: translator account, language verification, payout | Class humanControlled: `Owner verifies language pair competence, disclosure, publishing permission, and invoice.`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No vault credential `` and no `process.env` key; `mission_credentials`=0 rows; `mission_payout_slots` still `unconfigured` → `provider_not_configured` + `payout_slot_not_verified`. Honest status from `connectorToReadiness()`.
- **13. Current readiness status**: `not_configured` (kind=EARNING_SOURCE, status=PERMITTED, api=False, req=True) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain creates Gengo tester/translator account with language verification + KYC, links PayPal/bank (use Payoneer). 2) Human must authorize target & disclosure. 3) No credential (apiPermitted true for Test.io) — store if available. 4) Verify payout slot.

### 32. Direct Client Research (`direct_client_research`)

- **1. Platform**: Direct Client Research — `https://www.upwork.com/freelance-jobs/research/` (Direct client research: lawful data-rights permission, offline deterministic validation, verified via payout-verification)
- **2. Account required**: No — `requiresOwnerAccount=false (direct-secure)`, human actions: lawful purpose verification, data-rights permission, non-sensitive check, delivery approval, invoice — class `paid_research_data`
- **3. Pakistan availability**: PayPal-dependent payout → NOT usable in PK (PayPal not available). Requires alternative rail confirmation on officialUrl.
- **4. KYC/identity required**: Only lawful-purpose verification, no platform KYC
- **5. Documents required**: Engagement letter + data-rights permission + site-ownership proof where relevant; no platform ID beyond client contract
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Client USD agreement → bank/Stripe/PayPal → verified | Bank wire / Stripe / PayPal → payout-verification then receiving rail credit; net USD only after independent verification. (evidence: Direct client research: lawful data-rights permission, offline deterministic validation, verified via payout-verification) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://www.upwork.com/freelance-jobs/research/`
- **9. Automation allowed**: YES — Autonomous execution permitted for bounded offline work on owner-authorized non-sensitive data; publishing/sending remains human-controlled, no bulk scraping. (apiPermitted=True, status=ACTIVE, class=paid_research_data)
- **10. Human-only actions**: lawful purpose verification, data-rights permission, non-sensitive check, delivery approval, invoice | Class humanControlled: `Owner verifies lawful purpose, dataset rights, non-sensitive only, scope and acceptance; approves delivery and invoice.`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No technical blocker for discovery, but **payout still blocked**: `mission_payout_slots` unconfigured (no active slot) and no verified settlement (`mission_settlement_verifications` 0 rows) → cannot credit `mission_ledger` until owner verifies payout rail. Also requires lawful-purpose / data-rights proof before first assignment.
- **13. Current readiness status**: `ready` (kind=EARNING_SOURCE, status=ACTIVE, api=True, req=False) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) No marketplace account — direct client relationship. Zain must provide lawful-purpose verification, data-rights permission, site-ownership proof where relevant, and scoped API credentials via vault (`POST /api/credentials` AES-256-GCM) — never in chat. 2) Configure payout slot (Stripe/Wise/ACH) via Mission dashboard. 3) After owner assigns one agent to one opportunity, agent may autonomously perform scoped offline work (code/research) — no spam, no scraping without permission.

### 33. Direct AI Implementation (client) (`direct_ai_implementation`)

- **1. Platform**: Direct AI Implementation (client) — `https://www.upwork.com/hire/ai-engineers/` (Client AI integration with disclosed assistance)
- **2. Account required**: No — `requiresOwnerAccount=false (direct-secure)`, human actions: repo/hosting credentials via vault, production deployment, compliance review, contract/payout — class `ai_implementation`
- **3. Pakistan availability**: Yes — direct client; payout via owner-configured rail (Stripe/ACH/Wise) — PK entrepreneur can invoice USD via Wise/Stripe where supported; verify Wise PK availability [help.wise.com](https://wise.com/help/).
- **4. KYC/identity required**: Only lawful-purpose verification, no platform KYC
- **5. Documents required**: Engagement letter + data-rights permission + site-ownership proof where relevant; no platform ID beyond client contract
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Client contract → payout | Platform/Stripe payout → payout-verification + receiving rail credit; net USD only after independent verification. (evidence: Client AI integration with disclosed assistance) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://www.upwork.com/hire/ai-engineers/`
- **9. Automation allowed**: YES — Permitted where contract allows AI assistance; agent performs implementation, owner handles credentials, production deployment and compliance review. (apiPermitted=True, status=ACTIVE, class=ai_implementation)
- **10. Human-only actions**: repo/hosting credentials via vault, production deployment, compliance review, contract/payout | Class humanControlled: `Owner approves data sources, model credentials via vault, production keys, compliance review, contract signing, payout.`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No technical blocker for discovery, but **payout still blocked**: `mission_payout_slots` unconfigured (no active slot) and no verified settlement (`mission_settlement_verifications` 0 rows) → cannot credit `mission_ledger` until owner verifies payout rail. Also requires lawful-purpose / data-rights proof before first assignment.
- **13. Current readiness status**: `ready` (kind=EARNING_SOURCE, status=ACTIVE, api=True, req=False) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) No marketplace account — direct client relationship. Zain must provide lawful-purpose verification, data-rights permission, site-ownership proof where relevant, and scoped API credentials via vault (`POST /api/credentials` AES-256-GCM) — never in chat. 2) Configure payout slot (Stripe/Wise/ACH) via Mission dashboard. 3) After owner assigns one agent to one opportunity, agent may autonomously perform scoped offline work (code/research) — no spam, no scraping without permission.

### 34. Direct Automation (client) (`direct_automation`)

- **1. Platform**: Direct Automation (client) — `https://www.upwork.com/freelance-jobs/automation/` (Automation on permitted APIs with owner-authorized access)
- **2. Account required**: No — `requiresOwnerAccount=false (direct-secure)`, human actions: scoped API credentials via vault, data flow approval, production credentials, payout — class `automation_services`
- **3. Pakistan availability**: Yes — direct client; payout via owner-configured rail (Stripe/ACH/Wise) — PK entrepreneur can invoice USD via Wise/Stripe where supported; verify Wise PK availability [help.wise.com](https://wise.com/help/).
- **4. KYC/identity required**: Only lawful-purpose verification, no platform KYC
- **5. Documents required**: Engagement letter + data-rights permission + site-ownership proof where relevant; no platform ID beyond client contract
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Client contract → payout | Stripe/PayPal/bank via marketplace; verified before treasury (same chain). (evidence: Automation on permitted APIs with owner-authorized access) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://www.upwork.com/freelance-jobs/automation/`
- **9. Automation allowed**: YES — Automation of owner-authorized systems is permitted; bulk unsolicited outreach, spam, or ToS-violating scraping is blocked and human-gated. (apiPermitted=True, status=ACTIVE, class=automation_services)
- **10. Human-only actions**: scoped API credentials via vault, data flow approval, production credentials, payout | Class humanControlled: `Owner grants scoped API credentials via vault, approves data flows, reviews idempotency, handles production credentials.`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No technical blocker for discovery, but **payout still blocked**: `mission_payout_slots` unconfigured (no active slot) and no verified settlement (`mission_settlement_verifications` 0 rows) → cannot credit `mission_ledger` until owner verifies payout rail. Also requires lawful-purpose / data-rights proof before first assignment.
- **13. Current readiness status**: `ready` (kind=EARNING_SOURCE, status=ACTIVE, api=True, req=False) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) No marketplace account — direct client relationship. Zain must provide lawful-purpose verification, data-rights permission, site-ownership proof where relevant, and scoped API credentials via vault (`POST /api/credentials` AES-256-GCM) — never in chat. 2) Configure payout slot (Stripe/Wise/ACH) via Mission dashboard. 3) After owner assigns one agent to one opportunity, agent may autonomously perform scoped offline work (code/research) — no spam, no scraping without permission.

### 35. Direct Consulting (advisory) (`direct_consulting`)

- **1. Platform**: Direct Consulting (advisory) — `https://help.contra.com/en/articles/9322763` (Consulting with disclosed AI-assisted research)
- **2. Account required**: No — `requiresOwnerAccount=false (direct-secure)`, human actions: engagement scope verification, source approval, recommendations review, client communication, invoice — class `consulting_advisory`
- **3. Pakistan availability**: Yes — direct client; payout via owner-configured rail (Stripe/ACH/Wise) — PK entrepreneur can invoice USD via Wise/Stripe where supported; verify Wise PK availability [help.wise.com](https://wise.com/help/).
- **4. KYC/identity required**: Only lawful-purpose verification, no platform KYC
- **5. Documents required**: Engagement letter + data-rights permission + site-ownership proof where relevant; no platform ID beyond client contract
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Advisory fee → bank/Stripe → verified | Bank wire / Stripe → payout-verification + receiving rail credit; net USD only after verification. (evidence: Consulting with disclosed AI-assisted research) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://help.contra.com/en/articles/9322763`
- **9. Automation allowed**: YES — Research/draft automation permitted; client-facing advice, recommendations, and delivery require human expert review and disclosure. (apiPermitted=True, status=ACTIVE, class=consulting_advisory)
- **10. Human-only actions**: engagement scope verification, source approval, recommendations review, client communication, invoice | Class humanControlled: `Owner verifies engagement scope, approves research sources, reviews recommendations, handles client communication and in`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No technical blocker for discovery, but **payout still blocked**: `mission_payout_slots` unconfigured (no active slot) and no verified settlement (`mission_settlement_verifications` 0 rows) → cannot credit `mission_ledger` until owner verifies payout rail. Also requires lawful-purpose / data-rights proof before first assignment.
- **13. Current readiness status**: `ready` (kind=EARNING_SOURCE, status=ACTIVE, api=True, req=False) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) No marketplace account — direct client relationship. Zain must provide lawful-purpose verification, data-rights permission, site-ownership proof where relevant, and scoped API credentials via vault (`POST /api/credentials` AES-256-GCM) — never in chat. 2) Configure payout slot (Stripe/Wise/ACH) via Mission dashboard. 3) After owner assigns one agent to one opportunity, agent may autonomously perform scoped offline work (code/research) — no spam, no scraping without permission.

### 36. Direct SEO/Marketing Audit (`seo_direct`)

- **1. Platform**: Direct SEO/Marketing Audit — `https://legal.hubspot.com/terms-of-service` (SEO audit with site ownership proof, no ranking guarantees)
- **2. Account required**: No — `requiresOwnerAccount=false (direct-secure)`, human actions: site ownership verification, outreach template approval (no spam), recommendations review — class `seo_marketing`
- **3. Pakistan availability**: PayPal-dependent payout → NOT usable in PK (PayPal not available). Requires alternative rail confirmation on officialUrl.
- **4. KYC/identity required**: Only lawful-purpose verification, no platform KYC
- **5. Documents required**: Government photo ID (passport or CNIC with English details), selfie/live photo, proof of address (utility bill/bank statement ≤180 days), tax ID
- **6. Tax/business requirements**: Pakistan: NTN (CNIC-based via FBR/IRIS), PSEB registration for 0.25% final income tax on IT exports (optional but recommended), PRC/e-PRС from bank; US platforms may require W-8BEN (no US business) or W-8BEN-E
- **7. Payout method**: Audit fee → Stripe/PayPal → verified | Stripe/PayPal → payout-verification. (evidence: SEO audit with site ownership proof, no ranking guarantees) — payoutVerifiable=True
- **8. API/connector available**: YES — read-only / owner-PAT where documented — `apiPermitted=True`, `officialUrl=https://legal.hubspot.com/terms-of-service`
- **9. Automation allowed**: YES — Research/audit automation permitted; outreach/link-building and publishing are human-controlled and consent-gated. (apiPermitted=True, status=ACTIVE, class=seo_marketing)
- **10. Human-only actions**: site ownership verification, outreach template approval (no spam), recommendations review | Class humanControlled: `Owner verifies site ownership, approves outreach templates (no bulk spam), reviews recommendations.`
- **11. Credentials/config needed**: No platform credential — lawful-purpose / site-ownership verification only (store via `POST /api/credentials` vault AES-256-GCM if needed); stored ONLY in `mission_credentials` vault (`POST /api/credentials`, owner session, `identity-lock.ts`), never in chat/env echo. Also need `mission_payout_slots` active verification (checks + attestation via `payout-verification.ts`)
- **12. Exact current blocker**: No technical blocker for discovery, but **payout still blocked**: `mission_payout_slots` unconfigured (no active slot) and no verified settlement (`mission_settlement_verifications` 0 rows) → cannot credit `mission_ledger` until owner verifies payout rail. Also requires lawful-purpose / data-rights proof before first assignment.
- **13. Current readiness status**: `ready` (kind=EARNING_SOURCE, status=ACTIVE, api=True, req=False) — derived via `providerReadinessSummary()`; DB has 0 rows so not degraded/ready except direct_*
- **14. EXACT action Zain must personally perform**: 1) Zain verifies site ownership (DNS/TXT) and approves outreach template (no spam per `customer-work.ts` suppression). 2) No seller account — direct audit fee. 3) Configure payout slot (Stripe/PayPal-bypass via Wise). 4) Agent performs audit after ownership proof.

## 3) Consolidated — “Zain must personally do these things” (no secrets in chat)

**All 36 EARNING_SOURCE are currently OWNER ACTION REQUIRED / not_configured (except 5 direct READY) — USD $0 until Zain completes:**

### A. One-time mission financial wiring (required for ANY payout to verify)

1. **Verify at least ONE payout slot (1-4)** in Mission Control (`GET /api/payout-slots`): open Mission Dashboard → `Configure Payout Slot` → enter `provider_ref` (bank/ Connect ID), `minPayoutCents`, `maxPayoutCents`, `currency=USD` → `ConfirmPayoutVerification` with checks `{ownershipVerified:true, microDepositVerified:true, docsUploaded:true, attestation:”I own this account”}` — see `src/mission/payout-verification.ts:sweepPayoutVerifications`. Until one slot is `active`, every settlement fails `payout_slot_not_verified` (even direct client).
2. **Decide reinvest policy** (optional): `POST /api/policy` `updatePolicy({reinvestShareBps:2500}, ownerId)` if you want auto-reinvest on verified net; default 0 = no reinvest.

### B. Per-platform owner account + KYC + payout (do NOT paste keys in chat)

For each platform Zain intends to use (pick 1-2 first — do not open 36 at once):

- **Upwork** (`upwork`): Create account at https://support.upwork.com/hc/en-us/articles/10408677826963 with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `UPWORK_PAT` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Fiverr** (`fiverr`): Create account at https://help.fiverr.com/hc/en-us/articles/360020164118 with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `FIVERR_API_KEY` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Freelancer.com** (`freelancer`): Create account at https://www.freelancer.com/api/docs with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `FREELANCER_OAUTH_TOKEN` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Toptal** (`toptal`): Create account at https://www.toptal.com/faq with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `TOPTAL_API_KEY` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Contra** (`contra`): Create account at https://help.contra.com/en/articles/9322763 with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `CONTRA_API_KEY` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **PeoplePerHour** (`peopleperhour`): Create account at https://www.peopleperhour.com/fee-schedule with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Guru** (`guru`): Create account at https://www.guru.com/help/ with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **HackerOne** (`hackerone`): Create account at https://docs.hackerone.com/programs/philosophy with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `HACKERONE_API_KEY` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Bugcrowd** (`bugcrowd`): Create account at https://www.bugcrowd.com/resource/what-is-bug-bounty/ with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `BUGCROWD_API_KEY` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **YesWeHack** (`yeswehack`): Create account at https://www.yeswehack.com/ with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Intigriti** (`intigriti`): Create account at https://www.intigriti.com/ with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Kaggle Competitions** (`kaggle`): Create account at https://www.kaggle.com/competitions with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `KAGGLE_API_KEY` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Topcoder** (`topcoder`): Create account at https://www.topcoder.com/thrive/articles/Topcoder%20Platform with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Devpost** (`devpost`): Create account at https://devpost.com/rules with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **RapidAPI** (`rapidapi`): Create account at https://rapidapi.com/developer/billing with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `RAPIDAPI_KEY` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **AWS Data Exchange** (`aws_data_exchange`): Create account at https://aws.amazon.com/data-exchange/ with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `AWS_DATA_EXCHANGE_TOKEN` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Gumroad** (`gumroad`): Create account at https://gumroad.com/help/article/13-getting-paid with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `GUMROAD_API_KEY` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Lemon Squeezy** (`lemon_squeezy`): Create account at https://docs.lemonsqueezy.com/help/getting-started/payouts with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `LEMON_SQUEEZY_API_KEY` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Etsy (digital)** (`etsy_digital`): Create account at https://www.etsy.com/help/article/479 with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Shopify App Store** (`shopify_app_store`): Create account at https://shopify.dev/docs/apps/launch/app-store-review with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Atlassian Marketplace** (`atlassian_marketplace`): Create account at https://developer.atlassian.com/platform/marketplace/ with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **WordPress Plugin Directory** (`wordpress_plugin`): Create account at https://developer.wordpress.org/plugins/ with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **GitHub Sponsors** (`github_sponsors`): Create account at https://docs.github.com/en/sponsors with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `GITHUB_TOKEN` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Open Collective** (`open_collective`): Create account at https://docs.opencollective.com/help/collectives with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `OPENCOLLECTIVE_API_KEY` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Udemy** (`udemy`): Create account at https://support.udemy.com/hc/en-us/articles/229604988 with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Preply** (`preply`): Create account at https://preply.com/en/question/how-do-tutors-get-paid-40260 with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Amazon Associates** (`amazon_associates`): Create account at https://affiliate-program.amazon.com/help/node/topic/GRXPHT8U84RAYDXZ with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `AMAZON_ASSOCIATE_TAG` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Awin** (`awin`): Create account at https://www.awin.com/us/publishers with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `AWIN_API_KEY` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **ShareASale** (`shareasale`): Create account at https://www.shareasale.com/info/ with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `SHAREASALE_API_KEY` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Test.io** (`testio`): Create account at https://test.io/become-a-tester/ with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Gengo** (`gengo`): Create account at https://gengo.com/translators/ with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.
- **Direct SEO/Marketing Audit** (`seo_direct`): Create account at https://legal.hubspot.com/terms-of-service with real identity (passport/CNIC), complete KYC (selfie + address + bank statement), link payout **via Payoneer/Wise/bank — PayPal not available in PK** (see §3), complete tax (NTN + W-8BEN where asked), publish profile/listing **manually** (no automation). Then in Mission: `POST /api/credentials` (owner session) store `` (vault AES-256-GCM, `src/mission/self-management.ts:storeCredential`) — paste there, never here. For `FREELANCER`: use `POST /api/freelancer/authorize` OAuth flow instead of raw token paste.

### C. Direct-client work (the 5 READY sources — fastest path without marketplace KYC)

- **Direct Client Research / AI Implementation / Automation / Consulting + SEO Direct** (`direct_client_research`, `direct_ai_implementation`, `direct_automation`, `direct_consulting`, `seo_direct`): **No marketplace account needed**. Zain must: (1) authorize the client data (own the data / site ownership proof via DNS/TXT), (2) approve lawful purpose + non-sensitive check, (3) upload scoped API/repo credentials to vault (`POST /api/credentials`) — e.g., client repo PAT, hosting credentials — not in chat, (4) verify payout slot (A above). Then assign one agent to one opportunity via `POST /api/earning/opportunities/:id/assign` — agent may then autonomously perform scoped offline work (research, code, audit) without spam/scraping.

### D. Human-only tasks Zain can never delegate to agents (per `humanOnlyActions`):

- Seller account creation, KYC/tax, listing/gig publish, bidding/proposal send, contract acceptance/negotiation, milestone acceptance, payout withdrawal (all marketplaces).
- Screening (Toptal), client matching, team identity (Kaggle), scope/authorization verification + target approval + report submission + disclosure coordination (bug bounties), data-rights verification + publishing + refunds/support + app store review submission, repo ownership + sponsor relationship, enrollment + disclosure + manual link placement, tester/translator language verification + live tutoring.
- All payout withdrawals and tax filings — agents cannot touch bank.

## 4) “Not an earning source” — must not be counted

- **GitHub** (`github`) — `kind=INFRASTRUCTURE` — Infrastructure: code hosting, ToS allows PAT/apps with owner consent — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **GitLab** (`gitlab`) — `kind=INFRASTRUCTURE` — Infrastructure: code hosting — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **Docker** (`docker`) — `kind=INFRASTRUCTURE` — Infrastructure: containers — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **AWS** (`aws`) — `kind=INFRASTRUCTURE` — Infrastructure: hosting/compute — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **Cloudflare** (`cloudflare`) — `kind=INFRASTRUCTURE` — Infrastructure: CDN/hosting — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **Vercel** (`vercel`) — `kind=INFRASTRUCTURE` — Infrastructure: preview hosting — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **Neon** (`neon`) — `kind=INFRASTRUCTURE` — Infrastructure: Postgres — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **Supabase** (`supabase`) — `kind=INFRASTRUCTURE` — Infrastructure: DB — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **OpenAI** (`openai`) — `kind=INFRASTRUCTURE` — Infrastructure: LLM — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **Anthropic Claude** (`anthropic`) — `kind=INFRASTRUCTURE` — Infrastructure: LLM — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **Stripe** (`stripe`) — `kind=PAYMENT_RAIL` — Payment rail: payouts via Stripe Connect, 46+ countries — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **PayPal** (`paypal`) — `kind=PAYMENT_RAIL` — Payment rail — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **Payoneer** (`payoneer`) — `kind=PAYMENT_RAIL` — Payment rail — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **Wise** (`wise`) — `kind=PAYMENT_RAIL` — Payment rail — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **Sentry** (`sentry`) — `kind=TOOL` — Tool: error tracking — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **Datadog** (`datadog`) — `kind=TOOL` — Tool: observability — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **Tavily Search** (`tavily`) — `kind=TOOL` — Tool: search API — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)
- **Example Not Earning (demo)** (`example_not_earning`) — `kind=NOT_AN_EARNING_SOURCE` — Explicitly not an earning source; used to test classification guards — infrastructure/payment/tool/demo, never earnings (see `src/mission/database-isolation.test.ts` isolation guard)

## 5) Evidence & verification notes

- Counts derived from `grep -c "kind:'EARNING_SOURCE'" src/mission/earning/platform-connectors.ts` → **36 EARNING_SOURCE** of **57 total connectors** (10 infra, 4 payment_rail, 3 tool, 3 restricted_human_only, 1 not_an_earning_source).
- `OPPORTUNITY_REGISTRY` 24 classes (verified/candidate/restricted) — 94+ integrations used as **infrastructure** only (GitHub, Docker, AWS, Cloudflare, Vercel, Neon, Supabase, OpenAI, Anthropic, Stripe, PayPal, Payoneer, Wise, Sentry, Datadog, Tavily) — integration ≠ opportunity (see `GAP_REPORT.md`).
- Provider docs checked: Fiverr PK via Payoneer [nsave.com](https://www.nsave.com/pakistan/fiverr), PayPal not in PK [survivezeal.com](https://survivezeal.com/paypal-pakistan/)[nsave.com](https://www.nsave.com/pakistan/paypal-alternative), Upwork PK [upwork.com](https://www.upwork.com/hire/front-end-developers/pk/), Payoneer holding USD [payoneer.com](https://www.payoneer.com/payoneer-account/). Others via `officialUrl` in `platform-connectors.ts` per-connector — re-verify before enrollment.
- DB proof 2026-09-20: `sqlite3 mission.db "SELECT name FROM sqlite_master"` → 95 tables including `mission_provider_readiness` (0 rows), `mission_credentials` (0 rows), `mission_payout_slots` (4 rows status=unconfigured), `mission_earning_engine_opportunities` (0 settlment_verified). `mission_ledger` seq hash-chained, no `revenue` with `status=received` and verifier `owner|provider-webhook|bank-statement` → `$0` verified.
- No API keys requested here. All credentials must be stored via `POST /api/credentials` (owner session, scrypt + AES-256-GCM, `mission_credentials` vault) or OAuth flows — never in chat/logs.
