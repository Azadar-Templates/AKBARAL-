# AKBARAL! mobile — environment template.
# Copy to mobile/.env and set the public production API origin.
# Never commit a real .env; the root .gitignore excludes it.
EXPO_PUBLIC_API_BASE_URL="https://api.akbaral.ai"
